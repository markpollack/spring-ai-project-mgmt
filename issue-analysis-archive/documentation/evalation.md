Now evaluate how well your predictions match the gold labels.

  Use these inputs:
  - `conservative_full_classification.json`: Your model predictions
  - `test_set.json`: Ground truth labels

  Compute:
  - Precision, recall, F1 score (micro average)
  - Precision, recall, F1 per label (top 10 labels by frequency)
  - Macro-averaged precision, recall, and F1
  ⎿  Interrupted by user

and Only compare against the labels listed in labels.json as it provides the full allowed list.

